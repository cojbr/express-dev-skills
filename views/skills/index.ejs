<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>Welcome to <%= title %>!</p>
      <% Skills.forEach( (t, idx) => { %>
        <li>
          <form id="delete-form" action="/skills/<%= idx %>?_method=DELETE" method="post">
            <button type="submit">X</button>
          </form>

          <a href="/skills/<%= idx %>"><%= skill.skills %></a>
        </li>
      <% }); %>
    </ul>

  <a href="/skills/new">Add To-Do Item</a>


  </body>
</html>
